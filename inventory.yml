homelab:
  hosts:
    datacore:
      ansible_host: 192.168.178.167
      ansible_port: "{{ ssh_port }}"
      ansible_user: "{{ username }}"
      ansible_connection: ssh
      ansible_ssh_private_key_file: ~/.ssh/homeserver